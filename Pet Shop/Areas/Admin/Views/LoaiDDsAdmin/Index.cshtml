@model IEnumerable<Pet_Shop.Models.LoaiDD>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin_PetShop.cshtml";
}

<h2>Loại đồ dùng thú cưng</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiDD)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiDD)
        </td>
        <td>
            @Html.ActionLink("Thêm", "Edit", new { id=item.MaLoaiDD }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.MaLoaiDD }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaLoaiDD })
        </td>
    </tr>
}

</table>
