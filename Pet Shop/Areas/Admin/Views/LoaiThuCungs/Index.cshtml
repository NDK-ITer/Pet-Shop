@model IEnumerable<Pet_Shop.Models.LoaiThuCung>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin_PetShop.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiTC }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiTC }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiTC })
        </td>
    </tr>
}

</table>
